# MiniMind 快速开始指南

## 📚 欢迎使用 MiniMind

MiniMind 是一个基于 TinyAI 框架的轻量级语言模型,仅 **26M 参数**,支持完整的训练和推理流程。本指南将帮助您在 **5分钟内**快速上手。

**适用场景**:
- 🎓 学习大语言模型原理
- 🔬 研究小参数模型训练
- 💻 资源受限环境部署
- 🚀 快速原型开发

---

## 🔧 环境准备

### 系统要求

| 组件 | 最低要求 | 推荐配置 |
|------|---------|---------|
| **JDK** | Java 17+ | Java 17+ |
| **Maven** | 3.6+ | 3.8+ |
| **内存** | 4GB | 8GB+ |
| **GPU** | 无 (CPU可运行) | NVIDIA GPU (可选) |

### 安装步骤

#### 1. 安装 Java 17

**Linux/Mac:**
```bash
# 使用 SDKMAN
sdk install java 17.0.9-tem

# 验证安装
java -version
```

**Windows:**
- 下载 [Oracle JDK 17](https://www.oracle.com/java/technologies/downloads/#java17)
- 配置环境变量 `JAVA_HOME`

#### 2. 安装 Maven

**Linux/Mac:**
```bash
# 使用包管理器
brew install maven  # Mac
sudo apt install maven  # Ubuntu

# 验证安装
mvn -version
```

**Windows:**
- 下载 [Apache Maven](https://maven.apache.org/download.cgi)
- 配置环境变量 `MAVEN_HOME`

#### 3. 克隆项目

```bash
# 克隆 TinyAI 项目
git clone https://github.com/your-org/TinyAI.git
cd TinyAI

# 编译项目
mvn clean install -DskipTests
```

---

## 🚀 5分钟快速开始

### 示例 1: 创建模型并生成文本

创建文件 `QuickStart.java`:

```java
import io.leavesfly.tinyai.minimind.model.MiniMindModel;
import io.leavesfly.tinyai.minimind.tokenizer.MiniMindTokenizer;
import java.util.*;

public class QuickStart {
    public static void main(String[] args) {
        // 1. 创建 Tokenizer
        MiniMindTokenizer tokenizer = MiniMindTokenizer
            .createCharLevelTokenizer(6400, 512);
        
        // 2. 创建模型 (Small 26M)
        MiniMindModel model = MiniMindModel.create("my-model", "small");
        model.setTraining(false);  // 推理模式
        
        System.out.println("✅ 模型创建成功!");
        System.out.println("📊 " + model.getDescription());
        
        // 3. 准备输入
        String prompt = "Hello, world!";
        List<Integer> tokens = tokenizer.encode(prompt, true, false);
        int[] tokenArray = tokens.stream().mapToInt(i -> i).toArray();
        
        // 4. 生成文本
        System.out.println("\n🔮 开始生成...");
        int[] output = model.generate(
            tokenArray,  // 提示词
            50,          // 最大生成长度
            0.7f,        // 温度
            0,           // Top-K (0=不使用)
            0.9f         // Top-P
        );
        
        // 5. 解码输出
        String result = tokenizer.decode(
            Arrays.stream(output).boxed().collect(Collectors.toList())
        );
        
        System.out.println("✨ 生成结果: " + result);
    }
}
```

**运行示例:**
```bash
# 编译
javac -cp "target/classes:..." QuickStart.java

# 运行
java -cp "target/classes:..." QuickStart
```

**预期输出:**
```
✅ 模型创建成功!
📊 MiniMind Language Model - small with 26M parameters

🔮 开始生成...
✨ 生成结果: Hello, world! [生成的文本...]
```

---

### 示例 2: 使用不同采样策略

```java
// 贪婪采样 (确定性)
int[] greedy = model.generate(tokenArray, 50, 0.0f, 0, 0.0f);

// 温度采样 (温和随机)
int[] temp = model.generate(tokenArray, 50, 0.7f, 0, 0.0f);

// Top-K 采样 (从前K个中选择)
int[] topk = model.generate(tokenArray, 50, 1.0f, 40, 0.0f);

// Top-P 采样 (Nucleus采样)
int[] topp = model.generate(tokenArray, 50, 1.0f, 0, 0.9f);

System.out.println("贪婪采样: " + tokenizer.decode(Arrays.asList(greedy)));
System.out.println("温度采样: " + tokenizer.decode(Arrays.asList(temp)));
System.out.println("Top-K: " + tokenizer.decode(Arrays.asList(topk)));
System.out.println("Top-P: " + tokenizer.decode(Arrays.asList(topp)));
```

---

### 示例 3: 使用 CLI 工具

MiniMind 提供命令行工具,快速体验各种功能:

```bash
# 查看帮助
./bin/minimind.sh help

# 文本生成
./bin/minimind.sh generate \
  --model small \
  --prompt "Hello, world!" \
  --max-tokens 100 \
  --temperature 0.7

# 交互式对话
./bin/minimind.sh chat --model small

# 训练模型
./bin/minimind.sh train-pretrain \
  --data /path/to/data \
  --batch-size 32 \
  --epochs 10
```

详见: [CLI工具指南](./CLI-GUIDE.md)

---

## 📖 完整训练流程

### 1. 准备训练数据

**预训练数据格式 (纯文本):**
```
data/
├── train.txt
└── valid.txt
```

**SFT数据格式 (JSONL):**
```json
{"instruction": "写一首诗", "input": "", "output": "春眠不觉晓..."}
{"instruction": "翻译", "input": "Hello", "output": "你好"}
```

**DPO数据格式 (JSONL):**
```json
{
  "prompt": "写一首诗",
  "chosen": "春眠不觉晓,处处闻啼鸟...",
  "rejected": "床前明月光..."
}
```

### 2. 预训练

```java
import io.leavesfly.tinyai.minimind.model.MiniMindModel;
import io.leavesfly.tinyai.minimind.training.pretrain.*;

// 创建模型
MiniMindModel model = MiniMindModel.create("pretrain-model", "small");

// 创建预训练配置
PretrainConfig config = new PretrainConfig();
config.setDataPath("/path/to/train.txt");
config.setBatchSize(32);
config.setLearningRate(3e-4f);
config.setNumEpochs(10);
config.setWarmupSteps(2000);
config.setMaxGradNorm(1.0f);
config.setSaveSteps(5000);
config.setCheckpointDir("./checkpoints/pretrain");

// 创建训练器并训练
PretrainTrainer trainer = new PretrainTrainer(config);
trainer.train(model);

System.out.println("✅ 预训练完成!");
```

### 3. 监督微调 (SFT)

```java
import io.leavesfly.tinyai.minimind.training.sft.*;

// 加载预训练模型
MiniMindModel model = MiniMindModel.create("sft-model", "small");
// model.load("checkpoints/pretrain/epoch_10.pt");

// 创建SFT配置
SFTConfig sftConfig = new SFTConfig();
sftConfig.setDataPath("/path/to/sft_data.jsonl");
sftConfig.setBatchSize(16);
sftConfig.setLearningRate(5e-5f);
sftConfig.setNumEpochs(3);
sftConfig.setLossOnOutputOnly(true);  // 仅计算输出部分损失

// 训练
SFTTrainer sftTrainer = new SFTTrainer(sftConfig);
sftTrainer.train(model);

System.out.println("✅ SFT微调完成!");
```

### 4. LoRA 微调

```java
import io.leavesfly.tinyai.minimind.training.lora.*;

// 创建LoRA配置
LoRAConfig loraConfig = new LoRAConfig();
loraConfig.setRank(8);              // LoRA秩
loraConfig.setAlpha(16.0f);         // 缩放因子
loraConfig.setDropout(0.1f);
loraConfig.setTargetModules(Arrays.asList("q_proj", "v_proj"));

// 应用LoRA
LoRAAdapter.applyLoRA(model, loraConfig);

// 训练 (参数量仅为原模型的1-2%)
SFTConfig sftConfig = new SFTConfig();
sftConfig.setLearningRate(1e-4f);  // LoRA可用更大学习率
sftTrainer.train(model);

// 保存LoRA权重
LoRAAdapter.save(model, "lora_weights.pt");

System.out.println("✅ LoRA微调完成!");
```

### 5. DPO 训练

```java
import io.leavesfly.tinyai.minimind.training.dpo.*;

// 加载SFT模型
MiniMindModel policyModel = MiniMindModel.create("dpo-policy", "small");
MiniMindModel refModel = MiniMindModel.create("dpo-ref", "small");
refModel.setTraining(false);  // 冻结参考模型

// 创建DPO配置
DPOConfig dpoConfig = new DPOConfig();
dpoConfig.setBeta(0.1f);      // KL惩罚系数
dpoConfig.setDataPath("/path/to/dpo_data.jsonl");
dpoConfig.setLearningRate(5e-6f);  // DPO用极小学习率

// 训练
DPOTrainer dpoTrainer = new DPOTrainer(dpoConfig, refModel);
dpoTrainer.train(policyModel);

System.out.println("✅ DPO训练完成!");
```

---

## 🔍 常见问题 FAQ

### Q1: 编译失败,提示找不到依赖?

**A:** 确保先编译 TinyAI 核心模块:

```bash
cd TinyAI
mvn clean install -DskipTests

# 然后编译 MiniMind
cd tinyai-model/tinyai-model-minimind
mvn compile
```

### Q2: 运行时内存不足?

**A:** 增加 JVM 堆内存:

```bash
export MAVEN_OPTS="-Xmx4g"
# 或运行时指定
java -Xmx4g -cp ... YourClass
```

### Q3: 生成结果是乱码?

**A:** 检查以下几点:
1. Tokenizer 是否正确初始化
2. 词汇表大小是否匹配
3. 是否使用了 BPE 训练的 Tokenizer

```java
// 使用字符级 Tokenizer (简单但效果有限)
MiniMindTokenizer tokenizer = MiniMindTokenizer
    .createCharLevelTokenizer(6400, 512);

// 或使用 BPE Tokenizer (推荐)
BPETrainer bpeTrainer = BPETrainer.trainBPE(corpus, 6400, 5000);
MiniMindTokenizer tokenizer = MiniMindTokenizer
    .fromBPETrainer(bpeTrainer, 512);
```

### Q4: 训练速度很慢?

**A:** 优化建议:
1. 增大 `batchSize`
2. 使用梯度累积
3. 启用混合精度训练
4. 使用 GPU (如果可用)

```java
config.setBatchSize(64);  // 增大批次
config.setGradientAccumulationSteps(4);  // 梯度累积
```

### Q5: 如何评估模型效果?

**A:** 使用困惑度 (Perplexity) 和准确率:

```java
// 计算困惑度
float perplexity = evaluator.calculatePerplexity(model, validDataset);
System.out.println("困惑度: " + perplexity);

// 计算准确率
float accuracy = evaluator.calculateAccuracy(model, testDataset);
System.out.println("准确率: " + accuracy);
```

### Q6: 模型参数量如何计算?

**A:** 使用配置类估算:

```java
MiniMindConfig config = model.getConfig();
long params = config.estimateParameters();
System.out.println("参数量: " + params / 1_000_000 + "M");

// Small: ~26M
// Medium: ~108M
// MoE: ~145M
```

### Q7: 如何保存和加载模型?

**A:** 使用 Model 的保存/加载方法:

```java
// 保存模型
model.save("checkpoints/my_model.pt");

// 加载模型
MiniMindModel loadedModel = MiniMindModel.create("loaded", "small");
loadedModel.load("checkpoints/my_model.pt");
```

### Q8: API 服务如何部署?

**A:** 使用内置的 API 服务器:

```bash
# 启动 API 服务
./bin/start-api.sh 8080

# 测试
curl -X POST http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "minimind",
    "messages": [{"role": "user", "content": "你好!"}],
    "max_tokens": 100
  }'
```

详见: [API服务指南](./API-GUIDE.md)

---

## 📚 进阶使用

### 1. 自定义模型配置

```java
// 创建自定义配置
MiniMindConfig config = new MiniMindConfig();
config.setVocabSize(8000);
config.setMaxSeqLen(1024);
config.setHiddenSize(512);
config.setNumLayers(12);
config.setNumHeads(8);
config.setFfnHiddenSize(2048);
config.setDropout(0.1f);
config.setActivationFunction("silu");
config.setUseRoPE(true);
config.setPreLayerNorm(true);

// 验证配置
config.validate();

// 创建模型
MiniMindModel customModel = new MiniMindModel("custom", config);
System.out.println("参数量: " + config.estimateParameters());
```

### 2. 使用 MoE 架构

```java
// 启用 MoE
MiniMindConfig config = MiniMindConfig.createMoEConfig();
config.setUseMoE(true);
config.setNumExperts(4);           // 4个专家
config.setNumExpertsPerToken(2);   // 每次激活2个

MiniMindModel moeModel = new MiniMindModel("moe-model", config);

// MoE 模型参数量更大,但激活参数量少
System.out.println("总参数: " + config.estimateParameters());
System.out.println("激活参数: " + config.estimateActiveParameters());
```

### 3. 训练 BPE Tokenizer

```java
import io.leavesfly.tinyai.minimind.tokenizer.BPETrainer;

// 准备语料
List<String> corpus = new ArrayList<>();
corpus.add("Hello, world!");
corpus.add("This is a test.");
// ... 添加更多文本

// 训练 BPE
BPETrainer bpeTrainer = BPETrainer.trainBPE(
    corpus,   // 语料
    6400,     // 词汇表大小
    5000      // merge次数
);

// 保存
bpeTrainer.save("./tokenizer_model");

// 加载
BPETrainer loaded = BPETrainer.load("./tokenizer_model");
MiniMindTokenizer tokenizer = MiniMindTokenizer
    .fromBPETrainer(loaded, 512);
```

### 4. 批量推理

```java
// 准备批量输入
List<String> prompts = Arrays.asList(
    "Hello, world!",
    "How are you?",
    "Nice to meet you."
);

// 编码
EncodedBatch batch = tokenizer.encodeBatch(prompts, true, 50);

// 推理
Variable logits = model.predict(new Variable(batch.toNdArray()));

// 解码
for (int i = 0; i < prompts.size(); i++) {
    // 提取每个样本的logits
    // ... 解码和打印
}
```

### 5. 自定义采样策略

```java
// 实现自定义采样器
public class CustomSampler {
    public int sample(float[] logits, float temperature) {
        // 应用温度
        for (int i = 0; i < logits.length; i++) {
            logits[i] /= temperature;
        }
        
        // Softmax
        float[] probs = softmax(logits);
        
        // 采样
        return sampleFromDistribution(probs);
    }
    
    private float[] softmax(float[] logits) {
        // 实现 softmax
    }
    
    private int sampleFromDistribution(float[] probs) {
        // 实现分类采样
    }
}
```

---

## 📖 更多资源

### 文档

- [API参考文档](./API参考.md) - 完整的API说明
- [MiniMind模型设计](./MiniMind模型设计.md) - 架构深入解析
- [CLI工具指南](./CLI-GUIDE.md) - 命令行工具使用
- [API服务指南](./API-GUIDE.md) - REST API部署

### 示例代码

位于 `src/test/java/examples/`:
- `Example01_ModelCreation.java` - 模型创建与推理
- `Example02_Pretrain.java` - 预训练流程
- `Example03_SFT.java` - SFT微调
- `Example04_LoRA.java` - LoRA微调
- `Example06_Generation.java` - 文本生成策略
- `Example07_Evaluation.java` - 模型评估

### 社区

- GitHub Issues: 报告问题和建议
- 讨论区: 技术交流和答疑

---

## 🎯 学习路径推荐

### 初级 (1-2周)

1. ✅ 完成快速开始示例
2. ✅ 理解 Tokenizer 原理
3. ✅ 学习文本生成流程
4. ✅ 掌握基本 API 使用

### 中级 (2-4周)

1. ✅ 理解 Transformer 架构
2. ✅ 学习预训练流程
3. ✅ 掌握 SFT 微调
4. ✅ 了解 LoRA 原理

### 高级 (4-8周)

1. ✅ 研究 DPO 算法
2. ✅ 学习 MoE 架构
3. ✅ 优化推理性能
4. ✅ 部署生产服务

---

## 🆘 获取帮助

遇到问题?

1. 查阅 [常见问题 FAQ](#-常见问题-faq)
2. 阅读 [API参考文档](./API参考.md)
3. 查看示例代码
4. 提交 GitHub Issue

---

**祝您使用愉快!** 🎉

如有任何问题或建议,欢迎反馈!
